<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Circular border -->
    <solid android:color="@android:color/transparent" />
    <corners android:radius="32dp" />  <!-- Make this half of the ImageView size for a perfect circle -->
    <stroke
        android:color="@android:color/black"/>  <!-- Border thickness -->
</shape>